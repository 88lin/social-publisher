# Amplipost — 补充指令

@../CLAUDE.md
@../AGENTS.md

## Agent 入口

发布相关的所有任务由 `.claude/agents/content-coordinator.md` 处理。

## 长期记忆

`../memory.md` 是 Agent 的长期记忆文件，记录历史发布数据、有效内容方向、用户偏好等。
每次发布任务开始时读取，结束后按需更新。

## 完整规格

详见 `../SPEC.md`。
